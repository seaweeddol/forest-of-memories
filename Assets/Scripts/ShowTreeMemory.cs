using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowTreeMemory : MonoBehaviour
{
    // when player hovers mouse over tree, change icon to tree icon to indicate they can interact

    // if they click, display the truncated memory in front of the player (will need to get player position, facing, etc)

    // if they stay in range of the tree, scroll through the memory?
    // or if they hover over the memory make the background more opaque and show the entire memory

    // if the player leaves tree range, fade out the memory

}
